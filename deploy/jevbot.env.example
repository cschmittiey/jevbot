# Copy to deploy/jevbot.env and fill in. Gitignored -- do not commit real values.
DISCORD_TOKEN_JEV=your_discord_bot_token

# Must match LAYA_API_KEY in deploy/laya.env, or laya-serve answers 401.
# Generate with: openssl rand -hex 32
LAYA_API_KEY=your_laya_bearer_key
